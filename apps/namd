#
# This is the app module for NAMD.
# Two hooks are expected.
#

# Function to collect application dedicated  metrics from finised jobs.
function Collect () {
}

# Function to plot metrics from finished jobs.
function Plot () {
}
